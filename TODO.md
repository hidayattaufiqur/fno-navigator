# TODO — FnO Navigator

Follow-ups from the 2026-08-17 pathfinder pipeline round (substrate + tax case, QA approved).
All items are queued, none in progress.

## Pathfinder pipeline follow-ups

- [x] **1,347 deferred missingTable relations** — reduced 1,347 → 970 (done 2026-08-22: two mirror-sync passes; disk mirror confirmed final at 12 models/12,974 tables — tarball byte-identical, no additional models available). Pass 1: 89 verified via declaring-side-only evidence + 1 field-set correction (TSTimesheetTable→TSTimesheetEntryTotalsPart Worker→Resource) → 1,257. Pass 2 (partial batch: 84 of 349 missing tables landed in `sync-20260822/AxTable`, 78 new names): 158 verified both-sides, 168 verdicts flipped, 10 candidate re-adds proved edge-duplicates of existing entries and were NOT re-added → 970. Remaining 970 blocked: ~648 both-endpoints-absent (265 tables still missing from Windows source), plus noRelation/phantom/composite classes now classified against real metadata. See `patch-manifest.json` sections.mirror_sync + sections.mirror_sync_2.
- [x] **~80 mirror-verified non-tax gaps** — SalesLine/PurchLine/CustInvoiceTrans relations flagged in patch-manifest related_notes; capture pass to fold them into the public dataset. (done 2026-08-21: walked all 200 relations across the 3 tables; 148 already present, 35 promoted via sections.non_tax_gaps.adds → dataset +32 map edges, 17 skipped with reasons; earlier "~80" was an overcount vs exact-key ground truth. Full QA green: golden re-pinned + sanity-checked, fixtures adjusted (wms bar top-10→top-15, documented), bench p95 190ms.)
- [x] **P3 product boundary: intersection/set semantics** — pre-posting tax legs (SalesLine > TaxGroupHeading > TaxGroupData > TaxTable and SalesLine > TaxItemGroupHeading > TaxOnItem > TaxTable) are walkable but rank outside top-50 (TaxTable generic −1 class cap, TaxGroupHeading hub −1, specificity bucket 1). Decide: single-path discovery tool with honest copy, or v2.1 set-semantics support. Ranking recalibration is deferred until this call. (done 2026-08-21: Option A — single-path ranking stays frozen; pinned "Known canonical path" fixture layer is the sanctioned home for intersection-style legs; legend copy updated to point readers there.)
- [x] **Golden pool pin** — golden contract runs maxResults=20 (level-cap-truncated, omits TaxTransExtensionTH) while assert/bench/UI run 50. Pin golden pairs to maxResults=50 so the lockfile mirrors what the app renders. (QA LOW, janus 2026-08-17) (done 2026-08-21: golden-pairs.json pinned to maxResults=50; golden-results.json regenerated; golden-test.mjs green.)
- [x] **License nuance decision** — public audit docs cite aggregate mirror verification counts (process documentation, not mirror data). If strict reading wanted, strip class counts to "classes withheld". (QA NOTE, janus 2026-08-17) (done 2026-08-21: Option B provenance sentence added to both audit docs)
- [x] **Fixture note doc fix** — `salesline-taxtable-posted-truth` note claims direct TaxTrans leaf at "rank 16"; measured rank 6 (maxResults=50) / rank 4 (golden pool). Correct or drop the number. (QA LOW, janus 2026-08-17) (done 2026-08-21: path-fixtures.json note corrected to the measured rank; assert-fixtures.mjs green.)

## Agent tooling handoffs (2026-08-21)

- [x] **Embed REVIEWING.md as review guardrail** — wire `/home/smolpanda/Fun/Projects/fno-navigator/REVIEWING.md` into the fno-navigator skill (or git skill) as the mandatory pre-approve rubric for PRs in this repo. The file is intentionally local-only and gitignored; never commit it.
- [x] **e2e UI probes as agent debug tool** — wrap `tests/e2e-legend-probe.mjs` + `tests/e2e-legend-width-probe.mjs` (Playwright) into an on-demand UI debugging skill/tool for Hermes, Nix, and Janus. Debug-only; never a CI gate. (Context moved out of ci.yml comments.)
